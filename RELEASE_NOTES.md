# World Clock v1.0.0

Windows 11 世界时钟首次发布。

- 独立的半透明圆角时钟卡片，可拖动并保存位置。
- 默认北京、迪拜、卢萨卡，支持添加其他 IANA 时区。
- 背景透明度 0–100%，滑块及数字输入，实时预览。
- 跟随系统、白天（浅色）、夜晚（深色）三种模式。
- 显示日期、星期和相对本地时间的时差，自动处理夏令时。
- 右键设置、始终置顶、系统托盘。

## 下载运行

下载 `WorldClock.exe` 并双击，使用 Windows 11 x64，无需安装 Python。
`WorldClock-v1.0.0-windows-x64.zip` 包含同一 EXE、使用说明和第三方许可证。

本版本未使用商业代码签名证书。首次发布的应用可能显示 Windows 发布者提示。

## 已知限制

- 半透明背景，无毛玻璃模糊。
- 独立悬浮窗口，不属于 Win+W 面板，不嵌入 Explorer 桌面。
- 日/月图标仅表示当地 06:00–18:00/其余时间，不代表天气或日出日落。
