# World Clock V2.0.0

面向 Windows 11 x64 的原生 C++ 桌面世界时钟。本次汇总 v1.1.0 之后的更新，替换为新的专属图标。

## 下载与升级

- 下载 `WorldClock-v2.0.0-windows-x64.exe` 直接运行，或下载 ZIP 解压后运行 `WorldClock.exe`。
- 先从托盘退出旧版，再启动新版。无需 Python、Qt 或 `_internal` 目录。
- 继续读取 `%APPDATA%\WorldClockPython\clocks.json`。原有城市、位置和外观设置会保留；透明度方向自动换算一次。
- 若已启用自启，升级或移动 EXE 后请关闭再开启「开机自启」，更新路径。

## 相比 v1.1.0 的主要更新

### 原生 C++ 与单文件 EXE

从 Python/PySide6 改写为 C++17 / Win32，静态链接 C++ 运行库。无需 Qt DLL，消除本程序对 QtCore 的加载依赖。EXE 约 695 KiB。

### 自绘磨砂与柔和圆角

背景截图 → 缩放模糊 → 颜色蒙版和高光渐变 → 抗锯齿圆角裁切 → `UpdateLayeredWindow` 整帧显示。使用自绘方案，不调用未公开的 Accent 接口。截图仅缓存于内存，不保存或上传。

刷新不再把卡片移出屏幕、隐藏或临时改成全透明；背景捕获和绘制在同一 UI 线程完成。修正右键命中和消息处理，保留鼠标拖动。

### 0–100 背景浓度

0 为背景完全透明，文字保持清晰；50 对应约 128 Alpha；100 为不透明背景。支持滑块、数字输入及托盘每 10% 一档。圆角使用抗锯齿覆盖率，支持关闭磨砂。

### 卡片排列与设置同步

- 两张卡片可水平/垂直排列；托盘可一键排列全部卡片。
- 拖动吸附、屏幕边缘保护和位置保存。
- 设置窗口可选择应用到当前卡片、指定卡片或全部卡片。
- 同步外观、置顶和显示项，保留各自城市、时区、天气缓存和位置。同步为一次性复制。

### 完整托盘菜单和新图标

新增或整合透明度、跟随系统/浅色/深色、颜色、9 个显示开关、置顶、磨砂、自启、城市添加/更换/移除、保存与导出设置。顶部快捷项应用到全部卡片，也可进入单卡子菜单。

使用项目作者提供的新 `icon.ico`，统一 EXE、托盘、设置窗口及城市窗口图标。

## 延续 v1.1.0 功能

默认置顶、开机自启开关、在线城市搜索、Open-Meteo 天气及当日日出日落、太阳/月亮切换、显示/隐藏秒、城市/地区/日期/星期/时差/天气/日出日落显示、自定义颜色及系统主题跟随、本地 JSON 保存与导出。天气请求不需要用户填写 API Key；数据和服务使用遵循 Open-Meteo / GeoNames 条款。

## 验证与已知限制

- 最终 EXE 已通过图标资源加载、分层渲染、Alpha 端点、抗锯齿边缘、太阳/月亮绘制、右键消息至设置窗口、连续 12 次刷新保持可见性/位置、对齐、同步隔离和设置保存检查。
- 当前隔离测试环境读取真实桌面返回错误 6，模糊链路采用标记过的测试图案验证；尚未完成真实桌面的视觉验证。截图失败时保留缓存或回退到颜色背景，不隐藏窗口。
- C++ 版本的实时天气/搜索测试此前在此环境返回 WinHTTP 12185（客户端证书私钥不可用），本次未重新验证通过。离线计时仍可用。
- 背景捕获排除分层窗口，其他分层应用可能不会进入磨砂背景。0 浓度时透明区域鼠标穿透，可右键文字或从托盘进入设置。
- 不迁移外部 DesktopClockWidget 项目的独立配置及双色渐变设置。本版未做商业代码签名。

请查看随附的 `WorldClock-v2.0.0-build-report.json` 与 `SHA256SUMS-v2.0.0.txt`。

## 源码与构建

完整 C++ 源码附在 `WorldClock-source-v2.0.0.zip`。安装 Visual Studio Build Tools 的 C++ 桌面开发及 Windows SDK，运行 `build.bat`，输出 `dist/WorldClock.exe`。

第三方许可见 `THIRD_PARTY_NOTICES.md`。天气：Open-Meteo；城市：GeoNames。
