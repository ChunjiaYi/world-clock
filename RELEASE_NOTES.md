# World Clock v1.1.0

修复旧版 EXE 的 `DLL load failed while importing QtCore` 启动问题：隔离打包环境、固定 Qt 依赖、清理 DLL 搜索路径并检查打包清单。

## 新增

- 托盘开机自启开关（默认关闭）。
- 联网天气，太阳/月亮按当地每天日出日落切换。
- 新卡片默认置顶，旧版设置自动迁移。
- 城市、地区、日期、星期、时差、天气、日出日落等显示开关。
- 托盘保存卡片位置与设置，支持导出 JSON。
- 联网城市搜索，显示同名城市的地区和时区。
- 秒数显示开关。
- 自定义卡片颜色 / 跟随系统，保留 0–100% 背景透明度。

天气异步更新；断网保留缓存并标记，不影响时钟。新增单实例保护，避免自启与手动启动产生重复卡片。

## 使用

下载 `WorldClock-v1.1.0-windows-x64.zip`，完整解压后双击 `WorldClock.exe`（Windows 11 x64，无需 Python）。必须保留 `_internal` 文件夹，不要单独移动 EXE。升级前从系统托盘退出旧版。右键卡片打开设置；右键托盘保存或启用开机自启。

完整 ZIP 附说明与第三方许可证；SHA256SUMS.txt 可核对下载完整性。

数据来源：Open-Meteo / GeoNames（CC BY 4.0）。无设备定位；仅查询所选城市。应用未做商业代码签名。
